# 🎨 UI 기능 상세

## 색상 로그 (ANSI to HTML)

### 개요

Terraform의 터미널 색상 출력을 그대로 웹 UI에서 표현합니다.

### 적용 범위

1. **실시간 로그** - 실행 중 출력
2. **Terraform Outputs** - 완료 화면의 결과 값 🆕

### 지원하는 색상

| ANSI 코드 | 색상 | 용도 |
|-----------|------|------|
| `[32m` | 🟢 초록 | 생성될 리소스 (`+ create`) |
| `[31m` | 🔴 빨강 | 삭제될 리소스 (`- destroy`) |
| `[33m` | 🟡 노랑 | 변경될 리소스 (`~ update`) |
| `[34m` | 🔵 파랑 | 정보성 메시지 |
| `[36m` | 🔷 시안 | 데이터 리소스 (`data`) |
| `[1m` | **굵게** | 제목, 중요 메시지 |
| `[0m` | 리셋 | 기본 스타일로 복귀 |

### 예시

**Terraform 출력:**
```
\x1B[32m+\x1B[0m aws_vpc.main
\x1B[33m~\x1B[0m aws_instance.web
\x1B[31m-\x1B[0m aws_s3_bucket.old
```

**화면 표시:**
```
+ aws_vpc.main          (초록색)
~ aws_instance.web      (노란색)
- aws_s3_bucket.old     (빨간색)
```

---

## Plan 결과 모달

### 개요

Terraform Plan 결과를 90% 크기의 모달 팝업으로 표시하여, 로그를 계속 볼 수 있도록 합니다.

### 특징

- **크기**: 화면의 90% (max-width: 900px)
- **배경**: 어두운 오버레이 (로그 화면이 뒤에 보임)
- **위치**: 중앙 정렬
- **스크롤**: 내용이 길면 모달 내부 스크롤

### 레이아웃

```
┌──────────────────────────────────┐
│ 📊 Terraform Plan 결과           │ ← 헤더
├──────────────────────────────────┤
│                                  │
│  ➕ 생성: 49                      │ ← 요약
│  🔄 변경: 0                       │
│  ➖ 삭제: 0                       │
│                                  │
│  ⚠️ 주의: Apply를 실행하면...    │ ← 경고
│                                  │
├──────────────────────────────────┤
│         [취소]  [✅ Apply 실행]   │ ← 액션
└──────────────────────────────────┘
```

### 동작

1. **Plan 완료** → 모달 자동 표시
2. **[취소]** → 모달 닫기, 로그 계속 확인
3. **[Apply 실행]** → 모달 닫고 즉시 Apply 시작

---

## 로컬 시간 표시

### 개요

로그의 타임스탬프를 사용자의 시간대(로컬 시간)로 표시합니다.

### 변경 사항

**이전 (UTC):**
```
[07:49:21] ZIP 파일 압축 해제 중...
```

**현재 (로컬 시간):**
```
[16:49:21] ZIP 파일 압축 해제 중...  ← 한국 시간 (UTC+9)
```

### 구현

```javascript
const now = new Date();
const hours = String(now.getHours()).padStart(2, '0');
const minutes = String(now.getMinutes()).padStart(2, '0');
const seconds = String(now.getSeconds()).padStart(2, '0');
const timestamp = `${hours}:${minutes}:${seconds}`;
```

---

## 컴팩트한 창 크기

### 개요

앱 창 크기를 축소하여 더 효율적인 화면 사용이 가능합니다.

### 크기

| 항목 | 이전 | 현재 |
|------|------|------|
| 기본 크기 | 1400 x 900 | **1000 x 700** |
| 최소 크기 | 1000 x 700 | **900 x 650** |
| 메뉴바 | 표시 | **자동 숨김** (Alt 키로 표시) |

### 장점

- ✅ 화면 공간 절약
- ✅ 다른 창과 함께 사용하기 편리
- ✅ 깔끔한 UI (메뉴바 없음)
- ✅ 필요시 Alt 키로 메뉴 접근 가능

---

## 변수 입력 UI

### 개요

`variables.tf`를 자동으로 파싱하여 GUI로 입력받습니다.

### 지원 타입

| Terraform 타입 | HTML 입력 타입 | 예시 |
|---------------|---------------|------|
| `string` | text | `"ap-northeast-2"` |
| `number` | number | `3` |
| `bool` | checkbox | `true` / `false` |
| `list(string)` | text (쉼표 구분) | `"a, b, c"` → `["a", "b", "c"]` |

### 기능

1. **자동 파싱**: `variables.tf` 읽기
2. **기본값 표시**: `terraform.tfvars.example` 값 자동 채움
3. **기본값 리셋**: "🔄 기본값으로 리셋" 버튼으로 초기값 복원
4. **작업 선택**: "🚀 배포하기" 또는 "🗑️ 리소스 삭제"
5. **유효성 검사**: 필수 값 (AWS Credentials) 확인

### 예시

```hcl
# variables.tf
variable "aws_region" {
  description = "AWS Region"
  type        = string
  default     = "ap-northeast-2"
}

variable "instance_count" {
  description = "Number of instances"
  type        = number
  default     = 3
}
```

**화면:**
```
┌──────────────────────────────────┐
│ aws_region                       │
│ AWS Region                       │
│ ┌──────────────────────────────┐ │
│ │ ap-northeast-2               │ │
│ └──────────────────────────────┘ │
│                                  │
│ instance_count                   │
│ Number of instances              │
│ ┌──────────────────────────────┐ │
│ │ 3                            │ │
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```

---

## 자동 ZIP 구조 감지

### 개요

ZIP 파일 내부 구조를 자동으로 분석하여 `.tf` 파일의 실제 위치를 찾습니다.

### 지원 구조

**케이스 1: 루트에 .tf 파일**
```
project.zip
├─ main.tf          ← 감지
├─ variables.tf
└─ outputs.tf
```

**케이스 2: 하위 폴더에 .tf 파일**
```
project.zip
└─ terraform-project/
   ├─ main.tf       ← 감지
   ├─ variables.tf
   └─ outputs.tf
```

**케이스 3: 깊게 중첩된 구조**
```
project.zip
└─ src/
   └─ infrastructure/
      └─ aws/
         ├─ main.tf  ← 감지
         └─ ...
```

### 알고리즘

1. ZIP 압축 해제
2. 모든 `.tf` 파일 경로 수집
3. 공통 부모 디렉토리 계산
4. 해당 디렉토리를 작업 디렉토리로 설정

---

## Terraform Destroy 기능 🗑️

### 개요

배포된 AWS 리소스를 안전하게 일괄 삭제할 수 있습니다.

### 사용 시나리오

- 테스트 환경 정리
- 비용 절감을 위한 리소스 삭제
- 재배포 전 클린업

### 동작 방식

```
완료 화면
  ↓
[🗑️ 리소스 삭제] 버튼 클릭
  ↓
⚠️ 삭제 확인 모달 표시
  - 삭제될 리소스 개수 표시
  - "경고: 되돌릴 수 없음" 메시지
  ↓
[삭제 실행] 버튼 클릭
  ↓
terraform destroy -auto-approve 실행
  ↓
실시간 로그로 삭제 진행 상황 표시
  ↓
삭제 완료 화면
```

### UI 요소

**완료 화면의 Destroy 버튼:**
```
┌──────────────────────────────────┐
│ 🎉 배포 완료!                    │
├──────────────────────────────────┤
│ [🗑️ 리소스 삭제] [로그 보기] [처음으로] │
└──────────────────────────────────┘
```

**삭제 확인 모달:**
```
┌──────────────────────────────────┐
│ ⚠️ 리소스 삭제 확인               │
├──────────────────────────────────┤
│ 경고: 이 작업은 되돌릴 수 없습니다! │
│                                  │
│ 모든 AWS 리소스가 영구적으로      │
│ 삭제됩니다.                      │
│                                  │
│ ⚠️ 삭제될 리소스: 49개           │
│                                  │
│        [취소]  [삭제 실행]        │
└──────────────────────────────────┘
```

### 안전 장치

1. **2단계 확인**: 버튼 클릭 → 모달 확인
2. **리소스 개수 표시**: 삭제될 리소스 명확히 표시
3. **되돌릴 수 없음 경고**: 명확한 경고 메시지
4. **실시간 로그**: 삭제 과정 투명하게 표시

### 기술 구현

```javascript
// tf-runner.js에서 destroy 지원
if (command === 'destroy') {
  args.push('-auto-approve');
}

// Destroy 결과 파싱
if (command === 'destroy') {
  const match = stdout.match(/Destroy complete!\s*Resources:\s*(\d+)\s*destroyed/i);
  // ...
}
```

### 제한사항

- Apply가 성공한 경우에만 버튼 표시
- 한 번 삭제하면 복구 불가
- Terraform state가 정상이어야 함

---

## Terraform Outputs 색상 표시 🎨

### 개요

완료 화면에 표시되는 Terraform Outputs도 색상으로 표현하여 가독성을 높입니다.

### 표시 방식

**화면 표시**: ANSI → HTML 변환 (색상 유지)
```javascript
const htmlOutputs = ansiToHtml(state.applyResult.terraformOutputs);
outputsDisplay.innerHTML = htmlOutputs;
```

**복사 기능**: ANSI 코드 제거 (깔끔한 텍스트)
```javascript
const outputsText = state.applyResult.terraformOutputs
  .replace(/\x1B\[[0-9;]*[a-zA-Z]/g, '');
navigator.clipboard.writeText(outputsText);
```

### 화면 예시

```
┌────────────────────────────────────────────────┐
│ 📊 Terraform Outputs                  [📋 복사] │
├────────────────────────────────────────────────┤
│ Outputs:                                       │
│                                                │
│ alb_dns_name = "myapp-alb-123.amazonaws.com"  │ 🟢 초록색
│ ecr_repository_url = "123.dkr.ecr.ap-northeast-2.amazonaws.com/myapp" │ 🟢 초록색
│ rds_endpoint = "myapp-db.abc.rds.amazonaws.com:5432" │ 🟢 초록색
└────────────────────────────────────────────────┘
```

### 복사 결과

```
Outputs:

alb_dns_name = "myapp-alb-123.amazonaws.com"
ecr_repository_url = "123.dkr.ecr.ap-northeast-2.amazonaws.com/myapp"
rds_endpoint = "myapp-db.abc.rds.amazonaws.com:5432"
```

### 특징

| 기능 | 방식 | 이유 |
|------|------|------|
| **화면 표시** | ANSI → HTML | 🎨 시각적으로 보기 좋음 |
| **복사** | ANSI 제거 | 📋 다른 곳에 붙여넣기 편리 |

### 사용자 경험

1. **화면 확인**: 색상으로 키와 값을 시각적으로 구분
2. **복사하기**: HTML 태그 없이 깔끔한 텍스트로 복사
3. **공유**: Slack, 이메일 등에 바로 붙여넣기 가능

---

## 스마트 진행률 표시 📊

### 개요

각 단계의 실행 상태에 따라 정확한 진행률을 표시합니다.

### 진행률 체계

**Apply 워크플로우:**
```
시작:     0%  ⚪
Init 시작: 10%  🔄
Init 완료: 33%  ✅
Plan 시작: 40%  🔄
Plan 완료: 66%  ✅
Apply 시작: 70%  🔄
Apply 완료: 100% ✅
```

**Destroy 워크플로우:**
```
시작:       0%  ⚪
Init 시작:   10%  🔄
Init 완료:   50%  ✅
Destroy 시작: 60%  🔄
Destroy 완료: 100% ✅
```

### 실패 처리

**특징**: 실패한 단계에서 진행률이 멈춤

```
Init 실패 예시:
📋 실행 단계
✅ ZIP Import
✅ AWS Configure
❌ terraform init     ← 에러!
⏸️ terraform plan
⏸️ terraform apply

진행률: 10%  ← 멈춤!
```

### 구현

```javascript
// 단계 시작
updateStepStatus('init', 'active');
updateProgress(10);  // 시작 진행률

// 실행
const result = await runTerraform('init', ...);

// 실패 체크 (진행률 업데이트 안 함)
if (!result.success) {
  updateStepStatus('init', 'error');
  throw new Error('...');  // 여기서 멈춤!
}

// 성공 (진행률 증가)
updateStepStatus('init', 'completed');
updateProgress(33);  // 성공했을 때만!
```

---

## 세션 기반 Temp 디렉토리 🗂️

### 개요

각 실행마다 타임스탬프 기반의 격리된 임시 디렉토리를 사용합니다.

### 디렉토리 구조

```
C:\Users\...\AppData\Local\Temp\terraform-runner\
├── 2025-11-22T16-30-45\
│   └── project\
│       ├── main.tf
│       └── ...
├── 2025-11-22T16-35-20\
│   └── project\
│       └── ...
└── 2025-11-22T17-00-00\
    └── project\
        └── ...
```

### 장점

1. **충돌 방지**: 동시에 여러 프로젝트 실행 가능
2. **추적 가능**: 타임스탬프로 실행 시점 확인
3. **자동 정리**: 24시간 이상 된 세션 자동 삭제
4. **디버깅**: 이전 세션 파일 유지로 비교 가능

### 자동 정리

```javascript
// ZIP 압축 해제 시 자동 실행
cleanupOldSessions(24);  // 24시간 이상 된 세션 삭제

// 예시 로그:
[16:00:00] Old session cleaned: 2025-11-21T10-00-00 (30h old)
[16:00:00] Cleaned 2 old session(s)
```

### 세션 ID 형식

```
2025-11-22T16-30-45
└─┬──┘ └┬┘ └┬┘ └┬┘
  │     │   │   └─ 초 (45)
  │     │   └───── 분 (30)
  │     └────────── 시 (16)
  └──────────────── 날짜 (2025-11-22)
```

---

## 참고 자료

- [Electron Documentation](https://www.electronjs.org/docs)
- [ANSI Escape Codes](https://en.wikipedia.org/wiki/ANSI_escape_code)
- [Terraform Variable Configuration](https://www.terraform.io/language/values/variables)
- [Terraform Destroy Command](https://www.terraform.io/cli/commands/destroy)

